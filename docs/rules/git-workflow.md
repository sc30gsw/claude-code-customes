# Gitワークフロー

Git操作に関するルールです。

## コミットメッセージフォーマット

```
<type>: <description>

<optional body>
```

### タイプ一覧

| タイプ | 説明 |
|-------|------|
| feat | 新機能 |
| fix | バグ修正 |
| refactor | リファクタリング |
| docs | ドキュメント |
| test | テスト |
| chore | 雑務 |
| perf | パフォーマンス改善 |
| ci | CI/CD |

### 例

```
feat: マーケット検索機能を追加

- セマンティック検索を実装
- フィルタリングオプションを追加
- ページネーションをサポート
```

## プルリクエストワークフロー

PR作成時:

1. コミット履歴全体を分析（最新コミットだけでなく）
2. `git diff [base-branch]...HEAD` で全変更を確認
3. 包括的なPRサマリーをドラフト
4. TODOを含むテストプランを含める
5. 新しいブランチの場合は `-u` フラグでプッシュ

## 機能実装ワークフロー

### 1. まず計画

- **planner** エージェントを使用して実装計画を作成
- 依存関係とリスクを特定
- フェーズに分解

### 2. TDDアプローチ

- **tdd-guide** エージェントを使用
- まずテストを書く (RED)
- テストを通過する実装 (GREEN)
- リファクタリング (IMPROVE)
- 80%+カバレッジを確認

### 3. コードレビュー

- コード作成後すぐに **code-reviewer** エージェントを使用
- CRITICALとHIGH問題に対処
- 可能な場合はMEDIUM問題を修正

### 4. コミット＆プッシュ

- 詳細なコミットメッセージ
- Conventional Commitsフォーマットに従う

## ブランチ戦略

```
main
  └── feature/xxx
  └── fix/xxx
  └── refactor/xxx
```

## 禁止事項

- ❌ `git push --force` を main/master に使用しない
- ❌ `git commit --amend` を明示的要求なしに使用しない
- ❌ `--no-verify` でフックをスキップしない
- ❌ 機密情報をコミットしない
- ❌ 大きなバイナリファイルをコミットしない

## 推奨事項

- ✅ 小さく頻繁なコミット
- ✅ 意味のあるコミットメッセージ
- ✅ PRは小さく保つ
- ✅ レビュー前にセルフチェック
- ✅ CIが通過することを確認
